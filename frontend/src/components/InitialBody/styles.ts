import { Box, styled, Card } from '@mui/material';

export const Container: any = styled(Box)({
    display: 'flex',
    backgroundColor:  '#000000',
});

export const ContainerSimbolo: any = styled(Box)({
});